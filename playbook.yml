---
- hosts: all
  connection: local
  sudo: yes

  roles:
    - common
    - hhvm
    - php-fpm
    - memcached
    - percona
    - nginx
    - wp-cli
    - { role: wordpress, enviro: hhvm, tags: [ 'wordpress' ] }
